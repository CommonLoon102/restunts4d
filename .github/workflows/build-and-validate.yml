name: Build and validate

# Checks C/H formatting, runs the host regression tests, builds six DOS
# executables and runs full physics plus sampled renderer replay comparisons.
# PR and release workflows both gate on this validation.
on:
  workflow_call:
    inputs:
      shards:
        description: >-
          Number of parallel replay jobs. A public repository on the free
          plan may run 20 jobs at once account wide, so 20 uses the whole
          allowance and any other workflow queues behind this one.
        type: number
        default: 20
      partitions-per-shard:
        description: >-
          Partitions each replay job runs concurrently. DOSBox slows down
          roughly in proportion to how far the partitions oversubscribe the
          runner's four vCPUs, so raising this needs replay-timeout-seconds
          raised to match.
        type: number
        default: 12
      replay-timeout-seconds:
        description: Seconds each physics executable gets per replay.
        type: number
        default: 30
      renderer-test-percentage:
        description: Percentage of golden replays sampled for renderer tests (integer 1-100).
        type: number
        default: 5
      renderer-timeout-seconds:
        description: Seconds each renderer executable gets per replay.
        type: number
        default: 120
    outputs:
      exe-artifact:
        description: Artifact holding the six built executables.
        value: ${{ jobs.build.outputs.exe-artifact }}

env:
  EXE_ARTIFACT: restunts-exes
  WINEDEBUG: -all
  # Keep wineboot from prompting to download Mono and Gecko.
  WINEDLLOVERRIDES: mscoree,mshtml=

jobs:
  formatting:
    name: C/H formatting
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7

      - name: Install pinned formatting tools
        run: python3 -m pip install -r tools/scripts/requirements-format.txt

      - name: Validate C/H formatting against .editorconfig
        run: ec

      - name: Validate C layout and braces with clang-format
        run: git ls-files -z 'src/*.c' 'src/*.h' | xargs -0 -r clang-format --dry-run --Werror

  plan:
    name: Plan the shards
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      shards: ${{ steps.plan.outputs.shards }}
      total: ${{ steps.plan.outputs.total }}
    steps:
      - name: Split the golden set into shards
        id: plan
        env:
          SHARDS: ${{ inputs.shards }}
          PARTITIONS_PER_SHARD: ${{ inputs.partitions-per-shard }}
          RENDERER_TEST_PERCENTAGE: ${{ inputs.renderer-test-percentage }}
        run: |
          set -u
          if (( SHARDS < 1 || PARTITIONS_PER_SHARD < 1 )); then
              echo "::error::shards and partitions-per-shard must both be positive"
              exit 1
          fi
          if [[ ! "$RENDERER_TEST_PERCENTAGE" =~ ^([1-9][0-9]?|100)$ ]]; then
              echo "::error::renderer-test-percentage must be an integer from 1 to 100"
              exit 1
          fi
          echo "shards=[$(seq 0 $(( SHARDS - 1 )) | paste -sd, -)]" >> "$GITHUB_OUTPUT"
          echo "total=$(( SHARDS * PARTITIONS_PER_SHARD ))" >> "$GITHUB_OUTPUT"

  host-tests:
    name: Host regression tests
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7

      - name: Compile and run the host tests
        run: bash src/restunts/tests/run-host-tests.sh

  build:
    name: Build executables
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    outputs:
      exe-artifact: ${{ steps.names.outputs.value }}
    steps:
      - uses: actions/checkout@v7

      - name: Publish the artifact name
        id: names
        run: echo "value=$EXE_ARTIFACT" >> "$GITHUB_OUTPUT"

      - name: Install Wine
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          # bcc.exe, tasm32.exe, make.exe and the bundled DOSBox.exe are all
          # 32 bit PE binaries, so the i386 side of Wine is required.
          sudo apt-get install -y --no-install-recommends wine wine32:i386 xvfb

      - name: "Create the Wine prefix and map S: to the checkout"
        run: |
          xvfb-run -a wineboot --init
          wineserver -w
          # Every makefile addresses its sources as s:\..., the drive letter
          # tools/mount_stunts_to_s.bat creates on Windows.
          mkdir -p "$HOME/.wine/dosdevices"
          ln -sfn "$GITHUB_WORKSPACE" "$HOME/.wine/dosdevices/s:"

      - name: Build the game and physics executables
        run: |
          xvfb-run -a wine cmd /D /S /C "S: && cd S:\src\restunts && makerepldump"

      - name: Build the renderer executables
        run: |
          xvfb-run -a wine cmd /D /S /C "S: && cd S:\src\restunts && makepixldump"

      - name: Collect the executables
        run: |
          set -u
          mkdir -p dist
          missing=0
          # makerepldump.bat never reports failure, and a fresh checkout carries
          # none of these files, so their presence is the build's pass criteria.
          for exe in RESTUNTS RESTUNTO REPLDUMP REPLDUMO PIXLDUMP PIXLDUMO; do
              built=$(find stunts src/restunts/dos src/restunts/repldump src/restunts/pixldump \
                  -maxdepth 1 -iname "$exe.exe" -print -quit)
              if [[ -z "$built" ]]; then
                  echo "::error::$exe.EXE was not produced by the build"
                  missing=1
                  continue
              fi
              cp -- "$built" "dist/$exe.EXE"
          done
          (( missing == 0 )) || exit 1
          ls -l dist

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.EXE_ARTIFACT }}
          path: dist/*.EXE
          if-no-files-found: error
          retention-days: 14

  replay:
    name: Replays (shard ${{ matrix.shard }})
    needs: [plan, build]
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(needs.plan.outputs.shards) }}
    steps:
      - uses: actions/checkout@v7

      - name: Install DOSBox-X
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends dosbox-x

      - uses: actions/download-artifact@v8
        with:
          name: ${{ needs.build.outputs.exe-artifact }}
          path: exes

      - name: Smoke test DOSBox-X
        run: |
          # Also lets DOSBox-X create its user configuration before several
          # copies of it start up at once.
          dosbox-x -silent -conf tools/scripts/dosbox.proc.conf \
              -c "mount c $RUNNER_TEMP" -c "c:" -c "exit"

      - name: Assemble the game directory
        run: |
          set -u
          game_dir="$RUNNER_TEMP/gamedir"
          mkdir -p "$game_dir"
          # rpl2state.sh treats its own directory as the game directory, which
          # is why the harness is copied next to the game data when it runs.
          cp -R stunts/. "$game_dir/"
          # Only the archived golden set belongs in the desync run. In
          # particular, remove replay files copied from stunts before the
          # archive is extracted in the next step.
          find "$game_dir" -maxdepth 1 -type f -iname '*.rpl' -delete
          cp tools/scripts/cars/*.RES tools/scripts/cars/*.3SH \
             tools/scripts/cars/*.VSH tools/scripts/cars/*.PVS "$game_dir/"
          cp tools/scripts/rpl2state.sh tools/scripts/dosbox.proc.conf "$game_dir/"
          cp exes/*.EXE "$game_dir/"
          chmod +x "$game_dir/rpl2state.sh"
          echo "GAME_DIR=$game_dir" >> "$GITHUB_ENV"
          # Keep renderer reports separate while sharing the same game inputs.
          renderer_dir="$RUNNER_TEMP/renderer"
          mkdir -p "$renderer_dir"
          ln -s "$game_dir" "$renderer_dir/stunts"
          cp tools/scripts/rpl2pixdump.ps1 tools/scripts/dosbox.proc.conf "$renderer_dir/"
          echo "RENDERER_DIR=$renderer_dir" >> "$GITHUB_ENV"

      - name: Extract the golden replays
        run: |
          unzip -q tools/scripts/rpls_golden/replays.zip -d "${GAME_DIR:?}"
          echo "Replays in the game directory: $(find "$GAME_DIR" -maxdepth 1 -type f -iname '*.rpl' | wc -l)"

      - name: Run this shard's physics partitions
        env:
          SHARD: ${{ matrix.shard }}
          PARTITIONS_PER_SHARD: ${{ inputs.partitions-per-shard }}
          TOTAL: ${{ needs.plan.outputs.total }}
          REPLAY_TIMEOUT_SECONDS: ${{ inputs.replay-timeout-seconds }}
        run: |
          set -u
          first=$(( SHARD * PARTITIONS_PER_SHARD ))
          pids=()
          partitions=()
          for (( k = 0; k < PARTITIONS_PER_SHARD; k++ )); do
              partition=$(( first + k ))
              # rpl2state.sh only creates its output file when it logs an error,
              # so create it up front to keep the artifact shape predictable.
              : > "$GAME_DIR/partition_$partition.txt"
              "$GAME_DIR/rpl2state.sh" "$partition" "$TOTAL" \
                  > "$GAME_DIR/log_$partition.txt" 2>&1 &
              pids+=("$!")
              partitions+=("$partition")
          done
          status=0
          for i in "${!pids[@]}"; do
              if ! wait "${pids[$i]}"; then
                  echo "::error::partition ${partitions[$i]} exited non-zero"
                  status=1
              fi
          done
          exit "$status"

      - name: Run this shard's renderer partitions
        env:
          SHARD: ${{ matrix.shard }}
          SHARDS: ${{ inputs.shards }}
          PARTITIONS_PER_SHARD: ${{ inputs.partitions-per-shard }}
          TOTAL: ${{ needs.plan.outputs.total }}
          RENDERER_TEST_PERCENTAGE: ${{ inputs.renderer-test-percentage }}
          RENDERER_TIMEOUT_SECONDS: ${{ inputs.renderer-timeout-seconds }}
        run: |
          set -u
          pids=()
          partitions=()
          for (( k = 0; k < PARTITIONS_PER_SHARD; k++ )); do
              # Interleave partition IDs across shards so any extra samples
              # are spread across machines as well as workers.
              partition=$(( k * SHARDS + SHARD ))
              : > "$RENDERER_DIR/partition_$partition.txt"
              # Sample globally before assigning to the global partition index.
              # Sampling within each physics partition would skew coverage.
              pwsh -NoLogo -NoProfile -NonInteractive \
                  -File "$RENDERER_DIR/rpl2pixdump.ps1" \
                  -Partition "$partition" -PartitionCount "$TOTAL" \
                  -RendererTestPercentage "$RENDERER_TEST_PERCENTAGE" \
                  -DosBoxTimeoutSeconds "$RENDERER_TIMEOUT_SECONDS" \
                  > "$RENDERER_DIR/renderer_log_$partition.txt" 2>&1 &
              pids+=("$!")
              partitions+=("$partition")
          done
          status=0
          for i in "${!pids[@]}"; do
              if ! wait "${pids[$i]}"; then
                  echo "::error::renderer partition ${partitions[$i]} exited non-zero"
                  status=1
              fi
          done
          exit "$status"

      - name: Collect this shard's results
        if: always()
        run: |
          mkdir -p partitions/physics partitions/renderer
          cp "${GAME_DIR:?}"/partition_*.txt "${GAME_DIR:?}"/log_*.txt partitions/physics/
          cp "${RENDERER_DIR:?}"/partition_*.txt \
              "$RENDERER_DIR"/renderer_log_*.txt partitions/renderer/

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: partitions-${{ matrix.shard }}
          path: partitions/
          # A shard that produced nothing is reported by the coverage check in
          # the report job, which says far more than a failed upload would.
          if-no-files-found: warn
          retention-days: 14

  report:
    name: Replay report
    needs: [plan, replay]
    if: always()
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          pattern: partitions-*
          merge-multiple: true
          path: partitions

      - name: Merge the partitions
        env:
          TOTAL: ${{ needs.plan.outputs.total }}
        run: |
          tools/scripts/rpl2statemerge.sh partitions/physics "$TOTAL" physics_all.txt
          tools/scripts/rpl2statemerge.sh partitions/renderer "$TOTAL" renderer_all.txt
          cat physics_all.txt renderer_all.txt > partitions_all.txt

      - uses: actions/upload-artifact@v7
        with:
          name: partitions_all
          path: partitions_all.txt
          retention-days: 14

      - name: Check for mismatches
        env:
          REPLAY_RESULT: ${{ needs.replay.result }}
          RENDERER_TEST_PERCENTAGE: ${{ inputs.renderer-test-percentage }}
        run: |
          set -u
          expected=$(unzip -Z1 tools/scripts/rpls_golden/replays.zip | \
              grep -Eic '[0-9]{4}\.rpl$')
          processed=$(cat partitions/physics/log_*.txt 2>/dev/null | grep -c '^Processing ' || true)
          renderer_expected=$(( (expected * RENDERER_TEST_PERCENTAGE + 99) / 100 ))
          renderer_processed=$(cat partitions/renderer/renderer_log_*.txt 2>/dev/null | \
              grep -c '^Processing renderer replay ' || true)
          errors=$(grep -c '' partitions_all.txt || true)

          types() {
              awk -F '|' '{
                  for (i = 1; i <= NF; i++) {
                      if ($i ~ /^type=/) { print substr($i, 6); next }
                  }
              }' partitions_all.txt | sort | uniq -c | sort -rn
          }

          {
              echo "## Replay validation"
              echo
              echo "| | |"
              echo "|---|---|"
              echo "| Replays in the golden set | $expected |"
              echo "| Physics replays processed | $processed |"
              echo "| Renderer sample percentage | $RENDERER_TEST_PERCENTAGE% |"
              echo "| Renderer replays expected | $renderer_expected |"
              echo "| Renderer replays processed | $renderer_processed |"
              echo "| Errors | $errors |"
          } >> "$GITHUB_STEP_SUMMARY"

          status=0

          if [[ "$REPLAY_RESULT" != "success" ]]; then
              echo "::error::a replay shard did not finish (result: $REPLAY_RESULT)"
              status=1
          fi

          if (( processed != expected )); then
              echo "::error::$processed of $expected replays were processed, so the run is incomplete"
              printf '\n:x: Only **%s** of **%s** replays were processed.\n' \
                  "$processed" "$expected" >> "$GITHUB_STEP_SUMMARY"
              status=1
          fi

          if (( renderer_processed != renderer_expected )); then
              echo "::error::$renderer_processed of $renderer_expected renderer replays were processed"
              printf '\n:x: Only **%s** of **%s** sampled renderer replays were processed.\n' \
                  "$renderer_processed" "$renderer_expected" >> "$GITHUB_STEP_SUMMARY"
              status=1
          fi

          if (( errors > 0 )); then
              echo "::error::$errors error(s) in partitions_all.txt"
              echo
              echo "Errors by type:"
              types
              echo
              echo "partitions_all.txt:"
              cat partitions_all.txt
              {
                  printf '\n:x: **%s error(s)** — the full report is in the `partitions_all` artifact.\n\n' "$errors"
                  echo '### Errors by type'
                  echo '```'
                  types
                  echo '```'
                  echo
                  echo '### partitions_all.txt (first 200 lines)'
                  echo '```'
                  head -n 200 partitions_all.txt
                  echo '```'
              } >> "$GITHUB_STEP_SUMMARY"
              status=1
          fi

          if (( status == 0 )); then
              echo "No mismatches: $processed physics and $renderer_processed renderer replays agree byte for byte."
              printf '\n:white_check_mark: **%s** physics and **%s** renderer replays agree byte for byte.\n' \
                  "$processed" "$renderer_processed" >> "$GITHUB_STEP_SUMMARY"
          fi

          exit "$status"
